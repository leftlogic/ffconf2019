// Need this to have the base css at the beginning of the generated style on development
/*eslint no-unused-vars: ["error", { "varsIgnorePattern": "css" }]*/
import css from 'raw-loader!../../css/index.scss';

const Hack = () => {
  return null;
};

export default Hack;
